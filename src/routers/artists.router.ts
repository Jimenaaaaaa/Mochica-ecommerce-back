import { Router as router } from 'express';

// Lo sigo luego
// export const artistsRouter = router();
// const controller = new ArtistsController(repo);
// artistsRouter.get('/', controller.getAll.bind(controller));
